# McGBot milestone card avatar

Place the McGBot profile image here as:

`mcgbot-avatar.png`

Or set `X_MILESTONE_MCGBOT_AVATAR_PATH` in `.env` to an absolute path.

Used on **bot** milestone data cards (`!testxmilestone bot`, live McGBot calls).
