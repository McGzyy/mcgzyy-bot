<!--
Scaffold-only. Keep this guide concise until the unified “update the guides” workflow is in place.
-->

## User Guide

### What the bot does
- Tracks called coins and monitors performance over time
- Provides scan-style intel to help you evaluate tokens

### How to call a coin
- `!call <ca>` — official call + tracking
- `!watch <ca>` — track without caller credit

### Trusted Pro calls (if enabled for you)
- `!procall <ca> | <title> | <why> | <risk?>`
- What “Trusted Pro Call” means (curated, higher-trust note)

### What stats mean
- Total calls, valid calls, average X, best X (high-level definitions)
- Why sample size matters

### How tracking works
- What gets tracked (market cap at call, ATH, current MC)
- Milestones and monitoring

### Roles and trust levels (overview)
- `approved`, `top_caller`, `trusted_pro` (plain-English meaning)
- What is manual vs what is performance-based

### X integration basics
- Verifying your X handle
- What `#call` / `#procall` do (when enabled)
- What the bot will (and won’t) do on X

